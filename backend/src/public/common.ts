export const jwtConstants = {
  secret: 'secret'
}
export enum Status {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  PENDING = 'pending',
}

export enum Type {
  MOVIE = 'movie',
  SERIES = 'webseries',
  TV = 'telivision',
}

export enum Role {
  USER = 'user',
  PROMOTER = 'promoter',
  ADMIN = 'admin',
}
