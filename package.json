{
  "name": "flix-root",
  "private": true,
  "description": "Root placeholder – backend and flixdb run independently now",
  "devDependencies": {},
  "scripts": {
    "postinstall": "echo 'Install dependencies separately: cd backend && npm i; cd flixdb && npm i'"
  }
}
